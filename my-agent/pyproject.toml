[project]
name = "my-agent"
version = "0.0.0"
description = "A Managed Deep Agent project."
requires-python = ">=3.11"
dependencies = [
    "managed-deepagents==0.5.3",
    "langchain-openai>=0.2",
    "httpx>=0.27",
]
